# Contrbuting

...

## Hacking

Set up the project with `pipenv`:

```sh
pipenv install --dev
```

Run the tests:

```sh
pipenv run python -m pytest
```

Run type/lint check:

```sh
pipenv run mypy pygeojson
```

Check formatting:

```sh
pipenv run black
```
